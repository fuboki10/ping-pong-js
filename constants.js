const ballSpeedX = 4; // Speed of the ball in the X direction
const ballSpeedY = 3; // Speed of the ball in the Y direction
const paddleSpeed = 7; // Speed of the paddles
const paddleWidth = 15; // Width of the paddles
const paddleHeight = 100; // Height of the paddles

export { ballSpeedX, ballSpeedY, paddleSpeed, paddleWidth, paddleHeight };